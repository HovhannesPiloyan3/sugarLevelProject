class User {
    constructor(username, email, fullName, diabetesType, password) {
        this.username = username;
        this.email = email;
        this.fullName = fullName;
        this.diabetesType = diabetesType;
        this.password = password;
    }
}

module.exports = User;
